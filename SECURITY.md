# セキュリティポリシー

## 対応範囲

このリポジトリは、Gitで追跡されているのに`.gitignore`にも一致するファイルの増加を
検出する小さなゲートです。秘密情報の完全検出、依存関係の脆弱性監査、GitHub設定の
検証は保証しません。

## 問題の報告

脆弱性、秘密情報、個人情報の混入を見つけた場合は、公開Issueへ値や再現用credentialを
書き込まないでください。リポジトリ管理者へ非公開の連絡経路で、影響範囲と安全に共有
できる再現手順だけを伝えてください。

## 安全上の前提

- 検査は既定でread-onlyです。
- 書き込みは`--update-baseline`で指定したbaselineファイルだけです。
- baseline更新、skip、hook変更は人間がdiffをレビューします。
- 検査成功をpush、公開、mergeの承認として扱いません。
